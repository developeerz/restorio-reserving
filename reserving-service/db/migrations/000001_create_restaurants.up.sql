CREATE TABLE IF NOT EXISTS "restaurants" (
	"restaurant_id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	"name" VARCHAR(255) NOT NULL,
	"address" TEXT NOT NULL,
	"phone_number" VARCHAR(31)
);